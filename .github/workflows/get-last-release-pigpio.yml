name: Get latest release version
on:
  push:
    paths:
      - .github/workflows/get-last-release-pigpio.yml
  schedule:
    - cron:  '0 10 * * *'
jobs:
  get-version:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
          
      - name: Fetch release version
        run: |
          # Use github API to get latest release (tag_name) and remove the 'v' from it
          RELEASE_VERSION=$( curl -sL https://api.github.com/repos/joan2937/pigpio/releases/latest | jq -r ".tag_name" | sed -e 's/^v//')
          echo "RELEASE_VERSION=$RELEASE_VERSION" >> $GITHUB_ENV
          echo Release version $RELEASE_VERSION
          echo $RELEASE_VERSION > release-versions/pigpio-latest.log
          
          
      - name: Check for modified files
        id: git-check
        run:  |
           echo github
           echo ${{ github }} | jq '.'
           echo env
           echo ${{ env }} | jq '.'
           echo secrets 
           echo ${{ secrets }} | jq '.'
           echo ::set-output name=modified::$([ -z "`git status --porcelain`" ] && echo "false" || echo "true")
      
      - name: Commit latest release version
        if: steps.git-check.outputs.modified == 'true'
        run: |
          git config --global user.name 'bot_${{ github.event_name }}'
          git config --global user.email 'bot_${{ github.event_name }}@users.noreply.github.com'
          git commit -am "New release version $RELEASE_VERSION"
          git push
